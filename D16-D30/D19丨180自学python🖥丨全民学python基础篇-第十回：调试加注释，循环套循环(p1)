x = 1
while x < 10 :

    a = x * 10 + 4
    b = 4 * 10 + x

    if a * 693 ==396 * b :
        print('X的数值是：',x)

    x = x + 1
===
# 理论上
x = 0

while x <= 999999 :

    # 下面根据x生成自动补零的6位字符串，如‘000001’
    a = "%06d" % x

    if 测试口令(a) == '成功' :
        print('该卡号的口令是：',a)

    x = x + 1
